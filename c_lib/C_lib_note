C语言基本单位：函数

用户定义标识符特点是：字母_开头，不能是保留字

负数以补码表示
赋值要带结束符号;，并且赋值时不能将定义赋值。
（）》算术》关系》逻辑》条件》赋值》
c语言没有bool运算符，表示逻辑值真用》0表示


整型也可以输入实型
siseof是关键字
signed是关键字






#include #define不是C语句

int类型占2个字节，short类型占2字节
double类型占8字节

unsigned类型占2个字节

y++会被执行，执行前的结果赋值

97不用引号，“”用于字符串，‘’字符
j在前一行已经被加1了
赋值运算从右向左，并且左边的值已经发生改变

！=才为关系运算符



a已经被赋值


a=011,为8进制，为10进制的9
null变量无定义
该题有问题
k为8进制
%06D格式不合法，%06为字宽，且若%06d输出为|-08567|
两个%%d合成一个%（d），%%%d合成%（%d）
定义不合法\092不是8进制
scanf不允许指定精度，没有%n这个控制符，scanf在输入无符号数只能%d控制


SB题

a/b*b+a%b==a
register建议编译器将该变量放入cpu寄存器中



int 2字节16位，float4字节32位，double8字节64位，char1字节1位
char c=‘b’+b，b=a，c=‘b’（‘b’的asci码为98）b为-1,所以c=97
int b=2+7%-4-'A'(2+3(取余数永远为正7%-4》3)-65（‘A’=65）)

（int）
C
while没有函数体{}，答案是运行不出来。加上{}后，i必为10




//
//
//
//
//
//
//
//
int num['10']类型不对
数组分配不可以省略列下标


main函数中定义的变量只能在函数体中有效
c
c
函数嵌套定义没有意义，可以嵌套调用
c
c
a
c
a？
a
a
a
d
a
d b:逗号运算结果为最后一个
c









































































































































b
d

b
a
d
a(\为元字符要用字符形式时要转意)
c
d
d
a
c
a
d
d
a
a
c
a
b
b
b
c
b
c
c
c
d
a
b
b
c
d
b
b
b
c
没有进行初始化就没有确定的值
c
d
c
d
c
c
d
c
d
b
c
d当赋的值不满数组空间自动置0
d
d
c
c
d
d
d
a
?a实参数组是地址，形参数组是指针变量
ca函数内隐含储存类别是auto
c函数的隐含储存类别是extern
db不可以嵌套定义，可以嵌套调用
b
?b形参只能是变量（实参可以为任何类型）
a函数的定义不加分号，且用（，）分隔参数
a？
d
？b：static在未被赋初值时会给0
？b
a
ac a：viod a（int b）{不能放在这 int b }d：以形参类型为准
b：函数类型，int，long，short，float 储存类别，auto，register，static，extern
d
c?d（形参只能是个符号）
cb建立函数的目的是提高程序的可读性
b
b?d
b
a
b
||c
b
b
a
b
cd：01|10 =》11（3）
ac ：c程序在执行前对预处理命令进行处理
d
d
c一行不能出现多个预处理命令，预处理没有定义参数类型
ac typedef的用法 typedef （函数类型）函数名称
cb（if是关键字）
c
a
b
^位或运算，&位与运算
d b：p的值被覆盖，a[10]没有保存“china”
d：中间的*是乘号，b：不可以将值赋值给地址b
c
ac 指针相加无意义，指针相减代表中间元素个数， <代表实际内存的高低位
d
b
d
cd c：int *P没有初值，一个无效地址，不能直接*P操作
c d：p2没有赋值，*p2报错，b：n没有赋值
c d：d[4]不能右值赋值要用memcpy（c,d,sizeof(d)）
d
b
?d“”的作用是1：申请空间存放字符串，2：在字符串末尾加\0,3：返回地址。s[8],s该地址已经指向数组首地址。（要用strcpy函数）
d
cd a:int *f{整型指针} b:int (*f)[5]{指针数组} c:int(*f)()[整形函数指针] d:int *f(x) [整型指针函数]
c?
b
d
?d
a
ftell（fp）得到文件位置指针相对文件首的偏移数目a
a rewind函数作用是回到文件开头
b feek（fs，off，where）
ad改变文件的位置指针，辅助实现顺序读写，辅助实现文件的随机读写
dc 第3个参数有0_SEEK_SET,1_SEEK_CUR,2_SEEK_END fseek(fp,-20L,2)
d
d
d
da
dc
c
a
cb wb+若文件不存在建立，rb+若文件不存在报错（该题要打开新的二进制）
ab正确返回文件指针，错误返回（0）null
cd
d
c
考点是关键字，FILE可以定义
d
a
b
fwrite函数，fputc放一个字符，fread读
feof函数若文件已经读完返回非0值，否则返回零。
a
a
c
c
d
c
d
a
ab逻辑真等价与！0的数
d
b
函数没有return且没有用void，返回一个不定值
ad只允许本源文件使用要定义为静态全局变量
d
bd储存类别只有静态和外部之分，函数内定义的变量默认作用在本函数内
d不能把共用体作为函数参数，也不能是函数带回共用题变量，可以使用共用体指针
a
b
d
case后只能是常量，或常量表达式
a
ca语言三种基本结构是顺序，选择，循环
d
b
d
b
d
a
c
a
a
d？
a
a
b
bc：经过编译连接生成。exe
a
c
b
a
d
b
c判断两个字符串是否相等只能用strcmp函数
ba
auto
指针不能乘除，不能把指针的地址赋值
d
c
num为2的时候已经打印num为3
d
ca语言子函数数值的变化不会影响到主函数
b
a？d有歧义
不一定显示给出，不给返回类型默认是整型，自定义函数在main函数以前可以不声明
？数组指针存放的是数组元素的地址
a
b？||运算左边为真右边不执行
a？
b
a
b
d
while的位置
switch循环体中，每个case要加break跳出循环，否则向下执行。
a
b
double a;（int）a是直接省略小数部分，a/a商还是double所用是6.000000
c取值要用输入分隔符分隔
c
a
a
b
c

--或者++运算结果不能是常量或者表达式
a

